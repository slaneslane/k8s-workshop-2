apiVersion: v1
kind: ResourceQuota
metadata:
  name: workshop-quota
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "6"
    persistentvolumeclaims: "4"
    requests.storage: 5Gi
